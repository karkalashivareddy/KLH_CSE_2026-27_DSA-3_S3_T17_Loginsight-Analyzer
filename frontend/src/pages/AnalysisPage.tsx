import { Link } from 'react-router-dom';
import { History, Network, Timer, Workflow } from 'lucide-react';
import { Card, PageHeader } from '../components/ui';

export default function AnalysisPage() {
  return <div className="page"><PageHeader eyebrow="Analysis" title="Analysis hub" description="The measured engines behind search, aggregation and the trace laboratory." /><div className="analysis-grid"><Link className="analysis-card" to="/algorithms"><h3><Workflow size={16} aria-hidden="true" /> Algorithm catalogue</h3><p>Browse the real algorithm inventory exposed by the backend, with query types, complexity bounds, canonical endpoints and trace availability.</p><span className="btn-link">Browse algorithms</span></Link><Link className="analysis-card" to="/benchmarks"><h3><Timer size={16} aria-hidden="true" /> Search benchmarks</h3><p>Run the same pattern against the dataset haystack through each matcher and inspect the measured result for that execution.</p><span className="btn-link">Run a benchmark</span></Link><Link className="analysis-card" to="/runs"><h3><History size={16} aria-hidden="true" /> Run sessions</h3><p>Open recorded executions and replay their real operation ledger over SSE.</p><span className="btn-link">Open run sessions</span></Link></div><Card title="How the product uses the engines"><p className="page-description">Product search executes the submitted pattern through the configured matcher, fuzzy suggestions use edit distance, message patterns are heuristic token normalization, and incidents use dataset-derived error windows. Measurements and methodology are shown where the backend returns them.</p><div className="quick-actions"><Link className="btn btn-sm" to="/analytics"><Network size={14} aria-hidden="true" /> View analytics</Link><Link className="btn btn-sm" to="/search">Open search</Link></div></Card></div>;
}
