/**
 * Custom data structures used to keep algorithm internals transparent (docs/02 §8.3):
 * CustomArrayList, CustomStack, CustomQueue, CustomTrie, and similar.
 *
 * <p>Phase 1 skeleton only - this package is populated together with the first algorithms that
 * require these structures (Phase 3 onward).</p>
 */
package com.loginsight.dsa.common;