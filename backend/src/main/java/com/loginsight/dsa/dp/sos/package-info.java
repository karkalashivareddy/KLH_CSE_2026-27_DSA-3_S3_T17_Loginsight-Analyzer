/**
 * Subset aggregation (DSA-3 Module 3): the SOS (sum-over-subsets) zeta transform, computing for every
 * mask the sum of values over all of its submasks in O(n · 2^n).
 */
package com.loginsight.dsa.dp.sos;
